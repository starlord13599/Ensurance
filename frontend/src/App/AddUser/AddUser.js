import { Typography } from '@material-ui/core';
import React from 'react';
import DrawerLayout from '../../layouts/DrawerLayout';

function AddUser() {
	return (
		<DrawerLayout>
			<Typography variant="h2">From Add user</Typography>
		</DrawerLayout>
	);
}

export default AddUser;
